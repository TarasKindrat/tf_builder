name: 'git-terraform-module-plugin'
version: '1.0.0'
description: 'plugin allows user to store created terraform modules to git repos'
runtime:
  main: 'main:GitTerraformPlugin'
requirements:
  -name: 'requirements'
  version: '1.0.0'

